// 1. Минимальные выражения с разными типами новых строк
val a = 1; val b = 2  // Без nl (одна строка)
val c = 3
val d = 4  // LF (\n)
val e = 5\rval f = 6  // CR (\r)
val g = 7\r\nval h = 8  // CRLF (\r\n)
val i = 9\fval j = 10 // FF (\f)

// 2. Комбинации новых строк
val k = 11\n\nval l = 12  // Двойной LF
val m = 13\r\rval n = 14  // Двойной CR
val o = 15\n\rval p = 16  // LF-CR комбинация

// 3. Новые строки в разных контекстах
class Test {
  def method1: Int = 42  // nl после объявления класса
  
  def method2: Int =  // nl в определении метода
    43
  
  def method3: Int = {
    44  // nl внутри блока
  }
}

val r = 1 +
  2  // nl после оператора

// 5. Игнорируемые контексты (в скобках)
val s = (1
  + 2)  // nl в круглых скобках

// 6. Строковые литералы и комментарии
val u = "Привет\nМир"  // nl в строке (должен игнорироваться)
/*
 Многострочный
 комментарий
*/ val v = 23

// 7. Граничные случаи с операторами
val w = 1
+ 2  // nl перед оператором (другой результат)

val x = 1 +
2  // nl после оператора

// 8. Пустые строки и пробелы
val y = 1\n  \n  \nval z = 2  // nl с пробелами

// 9. Конец файла без новой строки
val end = 99